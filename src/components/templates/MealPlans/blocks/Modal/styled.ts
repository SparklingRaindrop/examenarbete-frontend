import styled from 'styled-components';

export const Wrapper = styled.div`
    width: 100%;

    position: fixed;
    bottom: 0;
    top: 30%;
    left: 0;
    right: 0;

    display: flex;
    flex-direction: row;
    align-items: center;
    gap: 1.5rem;
`;